<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="ProductTemplate">
        <Border Background="#D3D3D3" Width="200" Height="200" CornerRadius="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="5*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Image Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Source="{Binding Path=ImgPath}"></Image>
                <TextBlock Grid.Column="2" Grid.Row="0" FontSize="20" Foreground="Black" Text="{Binding StringFormat=ID: {0},Path=ID}"></TextBlock>
                <Button Grid.Column="3" Grid.Row="1" Style="{StaticResource WhiteButtonWithoutBorder}"></Button>
            </Grid>
        </Border>
    </DataTemplate>
</ResourceDictionary>